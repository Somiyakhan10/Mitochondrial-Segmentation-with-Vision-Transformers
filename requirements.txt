# Deep learning
torch==2.3.1
torchvision==0.18.1
monai==1.3.2

# Image I/O and processing
opencv-python==4.10.0.84
scikit-image==0.24.0
tifffile==2024.7.2
numpy==1.26.4
scipy==1.13.1

# CZI support (optional, not installed by default): aicsimageio's dependency
# tree (via ome-zarr -> fsspec[s3] -> s3fs/aiobotocore/botocore) pulls in a
# huge, loosely-pinned graph that makes pip's resolver backtrack for a very
# long time alongside the rest of this file. TIFF/OME-TIFF (the primary
# format, FR-01) doesn't need it. Install separately if CZI loading is
# needed: pip install aicsimageio==4.14.0

# Classical ML
scikit-learn==1.5.1

# Visualization
matplotlib==3.9.2
seaborn==0.13.2
plotly==5.23.0

# Dashboard
streamlit==1.37.0

# Experiment tracking
mlflow==2.15.1

# Explainable AI
captum==0.7.0
shap==0.46.0

# Reporting / export
reportlab==4.2.2
pandas==2.2.2
openpyxl==3.1.5

# Config, CLI, utils
pyyaml==6.0.1
click==8.1.7
tqdm==4.66.4
